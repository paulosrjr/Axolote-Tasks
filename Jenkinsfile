pipeline {
    agent { docker { image 'python:3.6' } }
    stages {
        stage('Build') {
            steps {
                sh 'python --version'
            }
        }
        stage('Test') {
            steps {
                sh 'python --version'
            }
        }
    }
}
